---
import Login from "@pages/login.astro"
---

<header class="w-full h-24 bg-slate-900 flex items-center justify-between px-6 shadow-md border-b border-white/10">

  <!-- LOGO -->
  <div class="flex items-center gap-3">
    <img src={`${import.meta.env.BASE_URL}logo.png`} alt="logo Alerta Dengue" class="h-16 w-auto">
  </div>

  <!-- TEXTO -->
  <h1 class="font-bitcount text-white text-2xl md:text-4xl font-semibold tracking-wide text-center">
    No dejes que se propague el dengue
  </h1>

  <a href={`${import.meta.env.BASE_URL}login`}
   class="flex items-center gap-2 bg-red-600 hover:bg-red-700 transition px-4 py-2 rounded-xl cursor-pointer shadow-md">
    <img src={`${import.meta.env.BASE_URL}person.svg`} alt="persona icon" class="h-5 w-5">
    <span class="text-white font-medium">Iniciar sesión</span>
  </a>




</header>